# Install Fastlane on macos

* Install rbenv and some prerequisites
  ```
  brew install rbenv
  brew install cmake
  brew install pkg-config
  ```
* Install ruby
  ```
  rbenv install 3.4.7
  rbenv global 3.4.7
  ```
* Install fastlane
  ```
  gem install fastlane
  ```
